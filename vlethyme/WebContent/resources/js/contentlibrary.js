define(['jquery', 'vle.core', 'angular'], function($, vle, angular) {
	angular.module('app').controllerProvider.register('LibraryController', ['$scope', '$route', '$routeParams', '$rootScope', '$modal', '$http',
    	  function ($scope, $route, $routeParams, $rootScope, $modal, $http) {
			$scope.getUserLibraries = function() {
   	    	
			};
	}]);
});